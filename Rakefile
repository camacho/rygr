task :install do
  `npm install -g coffee-script`
  `npm install -g bower`
  `npm install -g requirejs`
  `npm install -g gulp`
  `npm install`
  `bower install --allow-root`
end

task :update do
  `npm install`
  `bower install --allow-root`
end
